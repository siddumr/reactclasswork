import React from 'react';
export default function TryCatch({friut})
{
    try{
        if(fruit==="Onion")
        {
            throw new Error(" not a fruit");
        }
    }
    catch(error)
    {
        console.log("not a fruit");
        doucumment
    }
}